<!DOCTYPE html>
<html lang="EN">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{% block title %}Cerita Kita{% endblock %}</title>

    <!-- Fonts -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Scripts -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/app.css" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    {% block header %}{% endblock %}

    <!-- Custom Master Styles -->
    <style>
        /* Global Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            color: #334155;
        }

        .wrapper {
            display: flex;
            min-height: 100vh;
            position: relative;
        }

        /* Sidebar Styles */
        #sidebar {
            min-width: 280px;
            max-width: 280px;
            background: linear-gradient(180deg, #2196F3 0%, #1976D2 100%);
            color: white;
            position: fixed;
            height: 100vh;
            overflow-y: auto;
            transition: all 0.3s ease;
            z-index: 999;
            box-shadow: 4px 0 15px rgba(0, 0, 0, 0.1);
        }

        #sidebar.collapsed {
            min-width: 80px;
            max-width: 80px;
        }

        /* Sidebar Brand */
        .sidebar-brand {
            padding: 2rem 1.5rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            background: rgba(255, 255, 255, 0.05);
            text-align: center;
        }

        #sidebar.collapsed .sidebar-brand {
            padding: 1.5rem 0.5rem;
        }

        .brand-logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            text-decoration: none;
            color: white;
        }

        #sidebar.collapsed .brand-logo {
            justify-content: center;
            gap: 0;
        }

        .brand-icon {
            width: 40px;
            height: 40px;
            background: linear-gradient(45deg, #FF9800, #F57C00);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.25rem;
            font-weight: 600;
        }

        .brand-text {
            transition: all 0.3s ease;
        }

        #sidebar.collapsed .brand-text {
            opacity: 0;
            transform: scale(0);
            width: 0;
            height: 0;
            overflow: hidden;
        }

        .brand-text h4 {
            margin: 0;
            font-weight: 700;
            font-size: 1.2rem;
        }

        .brand-text p {
            margin: 0;
            font-size: 0.8rem;
            opacity: 0.8;
        }

        /* Sidebar Navigation */
        .sidebar-nav {
            padding: 1rem 0;
        }

        .nav-item {
            margin: 0.25rem 1rem;
        }

        #sidebar.collapsed .nav-item {
            margin: 0.25rem 0.5rem;
        }

        .nav-link {
            display: inline-flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem 1.25rem;
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            border-radius: 10px;
            transition: all 0.3s ease;
            font-weight: 500;
            width: 100%;
            /* Pastikan semua dalam satu baris */
            flex-direction: row;
            flex-wrap: nowrap;
            white-space: nowrap;
            vertical-align: middle;
            font-size: 1rem;
        }

        #sidebar.collapsed .nav-link {
            justify-content: center;
            padding: 1rem 0.75rem;
            gap: 0;
        }

        .nav-link:hover {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            transform: translateX(5px);
        }

        #sidebar.collapsed .nav-link:hover {
            transform: scale(1.1);
        }

        .nav-link.active {
            background: linear-gradient(45deg, #FF9800, #F57C00);
            color: white;
            box-shadow: 0 4px 15px rgba(255, 152, 0, 0.3);
        }

        .nav-icon {
            width: 22px;
            height: 22px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 1.25rem;
            /* Pastikan icon tidak mengecil */
            flex-shrink: 0;
            vertical-align: middle;
        }

        .sidebar-nav-text {
            transition: all 0.3s ease;
            /* Pastikan text tidak wrap */
            white-space: nowrap;
            overflow: hidden;
            display: inline-block;
            vertical-align: middle;
            line-height: 1.2;
            font-size: 1rem;
            font-weight: 500;
        }

        #sidebar.collapsed .sidebar-nav-text {
            opacity: 0;
            transform: scale(0);
            width: 0;
            height: 0;
            overflow: hidden;
        }

        /* Content Area */
        #content {
            flex: 1;
            margin-left: 280px;
            transition: all 0.3s ease;
            min-height: 100vh;
        }

        #content.collapsed {
            margin-left: 80px;
        }

        /* Top Navigation */
        .navbar {
            background: white !important;
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.08);
            border-bottom: 1px solid #e2e8f0;
        }

        .navbar-brand {
            margin-right: 1rem;
        }

        #sidebarCollapse {
            border: none;
            background: linear-gradient(45deg, #2196F3, #1976D2);
            color: white;
            border-radius: 8px;
            padding: 0.5rem 0.75rem;
            transition: all 0.3s ease;
        }

        #sidebarCollapse:hover {
            background: linear-gradient(45deg, #1976D2, #1565C0);
            transform: scale(1.05);
        }

        .page-title {
            font-weight: 700;
            color: #1e293b;
            margin: 0;
            font-size: 1.5rem;
        }

        /* User Dropdown */
        .user-dropdown {
            position: relative;
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 1rem;
            color: #64748b;
            text-decoration: none;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .user-info:hover {
            background: #f1f5f9;
            color: #2196F3;
        }

        .user-avatar {
            width: 32px;
            height: 32px;
            background: linear-gradient(45deg, #2196F3, #1976D2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
            font-size: 0.9rem;
        }

        .dropdown-menu {
            border: none;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
            border-radius: 10px;
            overflow: hidden;
        }

        .dropdown-item {
            padding: 0.75rem 1rem;
            transition: all 0.3s ease;
        }

        .dropdown-item:hover {
            background: linear-gradient(45deg, #2196F3, #1976D2);
            color: white;
        }

        /* Main Content */
        main {
            padding: 2rem;
            min-height: calc(100vh - 80px);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            #sidebar {
                margin-left: -280px;
            }

            #sidebar.mobile-active {
                margin-left: 0;
            }

            #content {
                margin-left: 0;
            }

            main {
                padding: 1rem;
            }
        }

        /* Custom Scrollbar */
        #sidebar::-webkit-scrollbar {
            width: 4px;
        }

        #sidebar::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.1);
        }

        #sidebar::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.3);
            border-radius: 2px;
        }

        #sidebar::-webkit-scrollbar-thumb:hover {
            background: rgba(255, 255, 255, 0.5);
        }

        /* Overlay for mobile */
        .sidebar-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 998;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
        }

        .sidebar-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        /* Loading States */
        .loading-spinner {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid #f3f3f3;
            border-top: 3px solid #2196F3;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Custom Alerts */
        .swal2-popup {
            border-radius: 15px;
            font-family: 'Inter', sans-serif;
        }

        .swal2-confirm {
            background: linear-gradient(45deg, #2196F3, #1976D2) !important;
            border-radius: 8px !important;
        }

        .swal2-cancel {
            background: linear-gradient(45deg, #6c757d, #5a6268) !important;
            border-radius: 8px !important;
        }

        /* Tooltip for collapsed sidebar */
        .nav-link {
            position: relative;
        }

        #sidebar.collapsed .nav-link::after {
            content: attr(data-tooltip);
            position: absolute;
            left: 100%;
            top: 50%;
            transform: translateY(-50%);
            background: rgba(0, 0, 0, 0.8);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 6px;
            font-size: 0.875rem;
            white-space: nowrap;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
            margin-left: 10px;
            z-index: 1000;
        }

        #sidebar.collapsed .nav-link:hover::after {
            opacity: 1;
            visibility: visible;
        }
    </style>
</head>

<body>
    <div id="app">
        <div class="wrapper">
            <!-- Sidebar Overlay for Mobile -->
            <div class="sidebar-overlay" id="sidebarOverlay"></div>
            
            <!-- Sidebar -->
            <nav id="sidebar">
                <!-- Brand Section -->
                <div class="sidebar-brand">
                    <a href="/dashboard" class="brand-logo">
                        <div class="brand-icon">
                            <i class="bi bi-book-half"></i>
                        </div>
                        <div class="brand-text">
                            <h4>Cerita Kita</h4>
                            <p>Berbagi Cerita</p>
                        </div>
                    </a>
                </div>

                <!-- Navigation Menu -->
                <ul class="sidebar-nav list-unstyled">
                    <li class="nav-item">
                        <a href="/dashboard" class="nav-link {{ routename == 'dashboard' ? 'active' : '' }}" data-tooltip="Dashboard">
                            <span class="nav-icon">
                                <i class="bi bi-house"></i>
                            </span>
                            <span class="sidebar-nav-text">Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/stories" class="nav-link {{ routename == 'stories' ? 'active' : '' }}" data-tooltip="Artikel">
                            <span class="nav-icon">
                                <i class="bi bi-journal-text"></i>
                            </span>
                            <span class="sidebar-nav-text">Artikel</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/categories" class="nav-link {{ routename == 'categories' ? 'active' : '' }}" data-tooltip="Kategori">
                            <span class="nav-icon">
                                <i class="bi bi-collection"></i>
                            </span>
                            <span class="sidebar-nav-text">Kategori</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/profile" class="nav-link {{ routename == 'profile' ? 'active' : '' }}" data-tooltip="Profile">
                            <span class="nav-icon">
                                <i class="bi bi-person"></i>
                            </span>
                            <span class="sidebar-nav-text">Profile</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/settings" class="nav-link {{ routename == 'settings' ? 'active' : '' }}" data-tooltip="Pengaturan">
                            <span class="nav-icon">
                                <i class="bi bi-gear"></i>
                            </span>
                            <span class="sidebar-nav-text">Pengaturan</span>
                        </a>
                    </li>
                </ul>

                <!-- Logout Section -->
                {#
                <div class="mt-auto p-3">
                    <a href="/logout" class="nav-link text-danger" data-tooltip="Logout">
                        <span class="nav-icon">
                            <i class="bi bi-box-arrow-left"></i>
                        </span>
                        <span class="sidebar-nav-text">Logout</span>
                    </a>
                </div>
                #}
            </nav>

            <!-- Main Content -->
            <div id="content">
                <!-- Top Navigation -->
                <nav class="navbar navbar-expand-lg sticky-top">
                    <div class="container-fluid">
                        <div class="navbar-brand">
                            <button type="button" id="sidebarCollapse" class="btn">
                                <i class="bi bi-list"></i>
                            </button>
                        </div>
                        
                        <h1 class="page-title flex-grow-1">
                            <i class="bi {{ pageicon }} me-2"></i>
                            {{ pagetitle }}
                        </h1>

                        <!-- User Dropdown -->
                        <div class="user-dropdown">
                            <div class="dropdown">
                                <a class="user-info dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <div class="user-avatar">
                                        {{ user.name|first|upper }}
                                    </div>
                                    <span class="d-none d-md-inline">{{ user.name }}</span>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li>
                                        <a class="dropdown-item" href="/profile">
                                            <i class="bi bi-person me-2"></i>
                                            Profile
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="/settings">
                                            <i class="bi bi-gear me-2"></i>
                                            Pengaturan
                                        </a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <a class="dropdown-item text-danger" href="/logout">
                                            <i class="bi bi-box-arrow-left me-2"></i>
                                            Logout
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </nav>

                <!-- Main Content Area -->
                <main class="container-fluid">
                    {% block content %}{% endblock %}
                </main>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
    <script>
        // Sidebar Toggle
        $('#sidebarCollapse').on('click', function () {
            if (window.innerWidth > 768) {
                // Desktop behavior: collapse/expand sidebar
                $('#sidebar').toggleClass('collapsed');
                $('#content').toggleClass('collapsed');
            } else {
                // Mobile behavior: show/hide sidebar
                $('#sidebar').toggleClass('mobile-active');
                $('#sidebarOverlay').toggleClass('active');
            }
        });

        // Close sidebar when clicking overlay (mobile)
        $('#sidebarOverlay').on('click', function () {
            $('#sidebar').removeClass('mobile-active');
            $(this).removeClass('active');
        });

        // Auto-close sidebar on mobile when clicking nav links
        $('.nav-link').on('click', function () {
            if (window.innerWidth <= 768) {
                $('#sidebar').removeClass('mobile-active');
                $('#sidebarOverlay').removeClass('active');
            }
        });

        // Handle window resize
        $(window).on('resize', function () {
            if (window.innerWidth > 768) {
                $('#sidebarOverlay').removeClass('active');
                $('#sidebar').removeClass('mobile-active');
            } else {
                $('#sidebar').removeClass('collapsed');
                $('#content').removeClass('collapsed');
            }
        });
    </script>

    {% if flash.success %}
    <script>
        Swal.fire({
            icon: 'success',
            title: 'Berhasil!',
            text: '{{ flash.success|e('js') }}',
            showConfirmButton: false,
            timer: 3000,
            toast: true,
            position: 'top-end'
        });
    </script>
    {% endif %}

    {% if flash.error %}
    <script>
        Swal.fire({
            icon: 'error',
            title: 'Error!',
            text: '{{ flash.error|e('js') }}',
            showConfirmButton: false,
            timer: 3000,
            toast: true,
            position: 'top-end'
        });
    </script>
    {% endif %}

    {% block script %}{% endblock %}
</body>
</html>