<footer class="footer-modern mt-auto">
    <div class="footer-content">
        <div class="container py-5">
            <div class="row">
                <div class="col-lg-4 col-md-6 mb-4 text-white">
                    <div class="footer-brand mb-4">
                        <a href="/" class="d-block mb-3">
                            <img src="/img/logo.png" alt="Cerita Kita" class="footer-logo">
                        </a>
                    </div>
                    <p class="text-light opacity-75 mb-4 footer-desc">
                        {{ footer_articles.0.content|raw }}
                        <!-- Platform berbagi cerita dan artikel berkualitas. Bergabunglah dengan komunitas penulis dan pembaca yang inspiratif. -->
                    </p>
                    <div class="social-links">
                        {% for item in social_media %}
                            <a href="{{ item.content|replace({'<p>': '', '</p>': ''}) }}" class="social-link" target="_blank"><i class="fab fa-{{ item.slug }}"></i></a>
                        {% endfor %}
                    </div>
                </div>
                
                <!-- Quick Links -->
                <div class="col-lg-2 col-md-6 mb-4">
                    <h6 class="text-white fw-bold mb-3 footer-title">NAVIGASI</h6>
                    <ul class="list-unstyled footer-links">
                        <li><a href="/">Beranda</a></li>
                        <li><a href="/stories">Daftar Cerita</a></li>
                        <li><a href="/login">Pendaftaran</a></li>
                    </ul>
                </div>
                
                <!-- Latest Articles -->
                <div class="col-lg-4 col-md-6">
                    <h6 class="text-white fw-bold mb-3 footer-title">ARTIKEL TERBARU</h6>
                    <div class="latest-articles">
                        <!-- {% for article in latestArticles %}
                        <a href="/article/{{ article.slug }}" class="article-item">
                            <div class="article-thumb">
                                <img src="/uploads/images/{{ article.image }}" alt="{{ article.title }}" onerror="this.src='/img/default-article.jpg'">
                            </div>
                            <div class="article-info">
                                <h6>{{ article.title|slice(0, 50) }}{% if article.title|length > 50 %}...{% endif %}</h6>
                                <span class="article-date">
                                    <i class="far fa-calendar-alt me-1"></i>{{ article.created_at|date('d M Y') }}
                                </span>
                            </div>
                        </a>
                        {% else %} -->
                        <div class="article-item">
                            <div class="article-info">
                                <h6>Belum ada artikel terbaru</h6>
                            </div>
                        </div>
                        <!-- {% endfor %} -->
                    </div>
                </div>
            </div>
        </div>
        
        <div class="footer-bottom">
            <div class="container">
                <div class="row align-items-center py-3">
                    <div class="col-md-6">
                        <p class="mb-0 text-light opacity-75">
                            © {{ "now"|date("Y") }} CeritaKita. All Rights Reserved.
                        </p>
                    </div>
                    <div class="col-md-6 text-md-end">
                        <div class="footer-bottom-links">
                            <a href="/privacy-policy" class="text-light opacity-75 me-3">Kebijakan Privasi</a>
                            <a href="/terms" class="text-light opacity-75">Syarat & Ketentuan</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</footer>
